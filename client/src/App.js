import './App.css';

function App() {
  return <div className = "App" >
    <
    label > Name < /label> <
  input type = "text" / >
    <
    label > age < /label>  <
  input type = "number" / >
    <
    label > country < /label> <
  input type = "text" / >

    <
    label > country < /label> <
  input type = "text" / >
    <
    label > wages(per year) < /label> <
  input type = "number" / >

    <
    /div>

}

export default App;